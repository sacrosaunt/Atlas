# Conversion-only dependencies. These are installed into Atlas's private build
# environment and are not imported by the app at runtime.
coremltools==9.0
huggingface-hub==0.36.2
numpy==1.26.4
safetensors==0.8.0
tokenizers==0.15.2
torch==2.7.0
transformers==4.38.2
